# Components

Components used across the application:

- `ContractForm`: Handles form state, validation, and saving contract data
- `Input`: Reusable input component with label, required flag, read-only, and error display
- `Button`: Primary button component
- `ErrorMessage`: Displays form validation errors
- `SuccessMessage`: Shows success message after form submission
